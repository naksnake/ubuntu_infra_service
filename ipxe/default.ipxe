
#!ipxe
# Default to Linux kernel+initrd (UEFI‑friendly)
set base http://${WEBFS_HOST_IP}:${WEBFS_PORT}
chain ${base}/ipxe/linux-kernel-initrd.ipxe || goto failed
:failed
echo Failed to chain linux-kernel-initrd.ipxe
shell
