syntax on                  " Enable syntax highlighting
set nocompatible           " Use Vim defaults instead of Vi compatibility
set shiftwidth=4           " Indent by 4 spaces
set tabstop=4              " Display tabs as 4 spaces
set softtabstop=4          " Number of spaces when pressing Tab
set expandtab              " Convert tabs to spaces

set ruler                  " Show cursor position
set backspace=2            " Allow backspacing over everything
set ic                     " Ignore case in searches
set ru                     " Show the current mode (redundant with ruler)
set hlsearch               " Highlight search results
set incsearch              " Show matches as you type
set smartindent            " Smart auto-indenting
set confirm                " Prompt to save changes before closing
set history=100            " Keep 100 lines of command history
set cursorline             " Highlight the current line
set nu                     " Show line numbers
set mouse=                 " Disable mouse support
colorscheme torte          " Set color scheme to 'torte'
